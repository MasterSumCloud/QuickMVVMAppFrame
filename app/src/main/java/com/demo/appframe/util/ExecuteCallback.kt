package com.demo.appframe.util

/*
import com.arthenica.ffmpegkit.FFmpegSession
import com.arthenica.ffmpegkit.FFmpegSessionCompleteCallback
import com.arthenica.ffmpegkit.SessionState

abstract class ExecuteCallback : FFmpegSessionCompleteCallback {
    override fun apply(session: FFmpegSession?) {
        if (session?.returnCode?.isValueSuccess == true) {
            sessionSuss(session)
        } else if (session?.returnCode?.isValueError == true) {
            sessionFail(session)
        } else if (session?.returnCode?.isValueCancel == true) {
            sessionCancel(session)
        }

        if (session?.state == SessionState.COMPLETED) {
            sessionComplete(session)
        }
    }

    abstract fun sessionSuss(session: FFmpegSession)
    open fun sessionFail(session: FFmpegSession) {}
    open fun sessionCancel(session: FFmpegSession) {}
    open fun sessionComplete(session: FFmpegSession) {}
}*/
